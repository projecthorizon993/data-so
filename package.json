{
  "name": "nim-chat-static",
  "version": "1.0.0",
  "type": "module",
  "description": "AURA campus screening assistant",
  "scripts": {
    "start": "node server.js",
    "dev": "PORT=3000 node server.js",
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
