# NIM runtime image reference — pin your model image here, keep template mounted
# so the prompt stays short and hot-swappable without rebuilding.
# Example (choose profile first: docker run --rm ... list-model-profiles):
#   image: nvcr.io/nim/meta/llama-3.1-8b-instruct:1.8.0
# See: NIM_CHAT_TEMPLATE + NIM_MAX_MODEL_LEN below.
